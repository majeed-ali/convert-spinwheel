// Deleted in favor of standalone /super-admin portal outside Shopify iframe.
import { redirect } from "react-router";
export const loader = async () => redirect("/app");
export default function Removed() { return null; }
